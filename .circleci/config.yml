version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - run: sudo npm install -sg "gatsby-cli@1.1.25"

      - restore_cache:
          key: v1-npm-{{ checksum "package-lock.json" }}
      - run: npm install -q
      - save_cache:
          key: v1-npm-{{ checksum "package-lock.json" }}
          paths:
            - node_modules

      - run: gatsby build --verbose --prefix-links
      - store_artifacts:
          path: public

workflows:
  version: 2
  build:
    jobs:
      - build
